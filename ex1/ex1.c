#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
	int distance = 100;

	// EXTRA CREDIT
	// When we screw around with the name of the
	// printf function the compiler thinks the
	// `distance` variable is a float rather than
	// an int.
	// rintf("You are %d miles away.\n", distance);

	// this is also a comment
	printf("You are %d miles away.\n", distance);



	return 0;
}
